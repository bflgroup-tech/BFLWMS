# CODEOWNERS — anyone listed here is auto-requested as a reviewer
# when a PR touches matching paths.
#
# Format: <path-pattern> <github-username-or-team>
# More specific patterns below override more general ones.
#
# Policy:
#   * PRs opened by @sheeja72 auto-merge on green CI (see
#     .github/workflows/automerge-sheeja.yml).
#   * PRs opened by anyone else (@Shyjeshk, @rijubfl, @shabeelap) can be
#     self-merged on green CI — GitHub disables the Approve button on your
#     own PR but the Merge button is enabled. Review is encouraged but not
#     required (Free plan has no server-side enforcement).
#   * PRs touching sensitive paths below require @sheeja72's review + merge
#     regardless of author.
# GitHub Free on a private repo can't server-enforce this — it's team
# discipline (see CONTRIBUTING.md).

# General code — any of the team can approve + squash-merge each other's PRs
*                                       @sheeja72 @Shyjeshk @rijubfl @shabeelap

# SQL schema needs an extra eye since it hits shared on-prem DBs
/db/                                    @sheeja72

# Auth + Program.cs changes need careful review — can break login for everyone
/src/Wms.Web/Auth/                    @sheeja72
/src/Wms.Web/Program.cs               @sheeja72

# CI / deploy workflows — controls what ships to production
/.github/workflows/                     @sheeja72
